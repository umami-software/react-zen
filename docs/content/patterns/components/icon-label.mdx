---
title: Icon Label
---

# Icon Label

A simple pattern for pairing an icon with text. This is one of the most common UI patterns and is easily composed using the `Row`, `Icon`, and `Text` components.

<Example>
  <IconLabel icon={<Lucide.Settings />} label="Settings" />

```jsx
<IconLabel icon={<Settings />} label="Settings" />
```
</Example>

## Source

```jsx
'use client';

import { Icon, Row, Text } from '@umami/react-zen';

export function IconLabel({
  icon,
  label,
  iconProps,
  labelProps,
  children,
  ...props
}) {
  return (
    <Row alignItems="center" gap="2" {...props}>
      <Icon {...iconProps}>{icon}</Icon>
      {label && <Text {...labelProps}>{label}</Text>}
      {children}
    </Row>
  );
}
```

## Variations

### Different sizes

<Example direction="column" gap="4">
  <IconLabel icon={<Lucide.Star />} label="Extra small" iconProps={{ size: 'xs' }} labelProps={{ size: 'xs' }} gap="1" />
  <IconLabel icon={<Lucide.Star />} label="Small" iconProps={{ size: 'sm' }} labelProps={{ size: 'sm' }} gap="1" />
  <IconLabel icon={<Lucide.Star />} label="Medium" iconProps={{ size: 'md' }} labelProps={{ size: 'md' }} />
  <IconLabel icon={<Lucide.Star />} label="Large" iconProps={{ size: 'lg' }} labelProps={{ size: 'lg' }} />

```jsx
<IconLabel icon={<Star />} label="Extra small" iconProps={{ size: 'xs' }} labelProps={{ size: 'xs' }} gap="1" />
<IconLabel icon={<Star />} label="Small" iconProps={{ size: 'sm' }} labelProps={{ size: 'sm' }} gap="1" />
<IconLabel icon={<Star />} label="Medium" iconProps={{ size: 'md' }} labelProps={{ size: 'md' }} />
<IconLabel icon={<Star />} label="Large" iconProps={{ size: 'lg' }} labelProps={{ size: 'lg' }} />
```
</Example>

### With colors

<Example direction="row" gap="6">
  <IconLabel icon={<Lucide.CheckCircle />} label="Success" iconProps={{ color: 'green' }} labelProps={{ color: 'green' }} />
  <IconLabel icon={<Lucide.XCircle />} label="Error" iconProps={{ color: 'red' }} labelProps={{ color: 'red' }} />
  <IconLabel icon={<Lucide.AlertTriangle />} label="Warning" iconProps={{ color: 'yellow' }} labelProps={{ color: 'yellow-600' }} />

```jsx
<IconLabel icon={<CheckCircle />} label="Success" iconProps={{ color: 'green' }} labelProps={{ color: 'green' }} />
<IconLabel icon={<XCircle />} label="Error" iconProps={{ color: 'red' }} labelProps={{ color: 'red' }} />
<IconLabel icon={<AlertTriangle />} label="Warning" iconProps={{ color: 'yellow' }} labelProps={{ color: 'yellow-600' }} />
```
</Example>

### As a button

<Example>
  <Button variant="outline">
    <IconLabel icon={<Lucide.Download />} label="Download" iconProps={{ size: 'sm' }} />
  </Button>

```jsx
<Button variant="outline">
  <IconLabel icon={<Download />} label="Download" iconProps={{ size: 'sm' }} />
</Button>
```
</Example>

### With children instead of label

<Example>
  <IconLabel icon={<Lucide.Mail />}>
    <Text>Messages</Text>
    <Text color="muted" size="sm">(3 unread)</Text>
  </IconLabel>

```jsx
<IconLabel icon={<Mail />}>
  <Text>Messages</Text>
  <Text color="muted" size="sm">(3 unread)</Text>
</IconLabel>
```
</Example>
