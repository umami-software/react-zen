---
title: Search Field
---

# Search Field

A specialized text input for search functionality, featuring a search icon and clear button. Provides familiar search UX patterns with keyboard support and accessible labeling.

<Example>
  <Box width="300px">
    <SearchField autoComplete="off" />
  </Box>

  ```jsx
  <SearchField />
  ```
</Example>

## Placeholder text

<Example>
  <Box width="300px">
    <SearchField placeholder="Enter some text" />
  </Box>

  ```jsx
  <SearchField placeholder="Enter some text" />
  ```
</Example>

## With delay

<Example>
  <Box width="300px">
    <SearchDelayExample />
  </Box>

  ```jsx
  <SearchField delay={300} onSearch={(value) => console.log(value)} />
  ```
</Example>

## Component props

<Example>
  <DataTable
    data={[
      { name: 'label', value: 'string', description: 'Label text displayed above the field' },
      { name: 'placeholder', value: 'string', description: 'Placeholder text' },
      { name: 'value', value: 'string', description: 'Controlled value' },
      { name: 'defaultValue', value: 'string', description: 'Default value (uncontrolled)' },
      { name: 'delay', value: 'number', description: 'Debounce delay in ms before onSearch fires (default: 0)' },
      { name: 'onChange', value: '(value: string) => void', description: 'Called on every change' },
      { name: 'onSearch', value: '(value: string) => void', description: 'Called after delay (debounced)' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>
