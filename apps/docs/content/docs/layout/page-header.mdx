---
title: Page Header
---

# Page Header

Provides a consistent title and action area for a page.

<Example>
  <PageHeader>
    <PageHeaderTitle
      title="Users"
      description="Manage team members and permissions."
    />
    <PageHeaderActions>
      <Button variant="outline">Export</Button>
      <Button variant="primary">Add user</Button>
    </PageHeaderActions>
  </PageHeader>

```jsx
import {
  Button,
  PageHeader,
  PageHeaderActions,
  PageHeaderTitle,
} from '@umami/react-zen';

<PageHeader>
  <PageHeaderTitle
    title="Users"
    description="Manage team members and permissions."
  />
  <PageHeaderActions>
    <Button variant="outline">Export</Button>
    <Button variant="primary">Add user</Button>
  </PageHeaderActions>
</PageHeader>
```
</Example>

## Components

### PageHeader

The outer layout. It accepts `Row` props.

### PageHeaderTitle

Displays the breadcrumb, title, description, and optional children. It accepts `Column` props.

| Prop | Type | Description |
|------|------|-------------|
| `title` | `ReactNode` | Page title |
| `description` | `ReactNode` | Supporting text |
| `breadcrumb` | `ReactNode` | Content displayed above the title |

### PageHeaderActions

Groups page-level actions. It accepts `Row` props.

## Examples

### With breadcrumb

<Example>
  <PageHeader>
    <PageHeaderTitle
      breadcrumb={
        <Row alignItems="center" gap="1">
          <Text size="sm" color="muted">Settings</Text>
          <Icon size="xs" color="muted"><Lucide.ChevronRight /></Icon>
          <Text size="sm" color="muted">Team</Text>
        </Row>
      }
      title="Team members"
      description="Invite and manage team members."
    />
    <PageHeaderActions>
      <Button variant="primary">Invite member</Button>
    </PageHeaderActions>
  </PageHeader>

```jsx
<PageHeader>
  <PageHeaderTitle
    breadcrumb={<Breadcrumbs />}
    title="Team members"
    description="Invite and manage team members."
  />
  <PageHeaderActions>
    <Button variant="primary">Invite member</Button>
  </PageHeaderActions>
</PageHeader>
```
</Example>

### Title only

<Example>
  <PageHeader>
    <PageHeaderTitle title="Dashboard" />
  </PageHeader>

```jsx
<PageHeader>
  <PageHeaderTitle title="Dashboard" />
</PageHeader>
```
</Example>
