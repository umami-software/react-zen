---
title: Popover
---

# Popover

A popover is an overlay element positioned relative to a trigger.

<Example>
  <DialogTrigger>
    <Button variant="outline">Settings</Button>
    <Popover>
      <Dialog>
        <Column gap="3">
          <Switch defaultSelected>Wi-Fi</Switch>
          <Switch defaultSelected>Bluetooth</Switch>
          <Switch>Mute</Switch>
        </Column>
      </Dialog>
    </Popover>
  </DialogTrigger>

  ```jsx
  <DialogTrigger>
    <Button variant="outline">Settings</Button>
    <Popover>
      <Dialog>
        <Column gap="3">
          <Switch defaultSelected>Wi-Fi</Switch>
          <Switch defaultSelected>Bluetooth</Switch>
          <Switch>Mute</Switch>
        </Column>
      </Dialog>
    </Popover>
  </DialogTrigger>
  ```
</Example>

## Component props

<Example>
  <DataTable
    data={[
      { name: 'placement', value: 'Placement', description: '"top" | "bottom" | "left" | "right" | "start" | "end"' },
      { name: 'offset', value: 'number', description: 'Distance from the trigger element' },
      { name: 'isFullscreen', value: 'boolean', description: 'Opens popover in fullscreen mode' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>
