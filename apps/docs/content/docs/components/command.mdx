---
title: Command
---

# Command

A command palette for searching and running actions with keyboard navigation.

<Example>
  <CommandExample />

  ```jsx
  <Command>
    <CommandInput placeholder="Type a command or search..." />
    <CommandList>
      <CommandEmpty>No results found.</CommandEmpty>
      <CommandGroup heading="Suggestions">
        <CommandItem icon={<Calendar />}>Calendar</CommandItem>
        <CommandItem icon={<Smile />}>Search emoji</CommandItem>
        <CommandItem icon={<Search />} isDisabled>
          Search web
        </CommandItem>
      </CommandGroup>
      <CommandSeparator />
      <CommandGroup heading="Settings">
        <CommandItem icon={<User />}>Profile</CommandItem>
        <CommandItem icon={<Settings />}>Settings</CommandItem>
      </CommandGroup>
    </CommandList>
  </Command>
  ```
</Example>

## Dialog

Use `CommandDialog` to display the command palette in a modal overlay. Control visibility
with the `isOpen` and `onOpenChange` props.

<Example>
  <CommandDialogExample />

  ```jsx
  function App() {
    const [isOpen, setIsOpen] = useState(false);

    return (
      <>
        <Button variant="primary" onPress={() => setIsOpen(true)}>
          Open command palette
        </Button>
        <CommandDialog isOpen={isOpen} onOpenChange={setIsOpen}>
          <CommandInput placeholder="Type a command or search..." />
          <CommandList>
            <CommandEmpty>No results found.</CommandEmpty>
            <CommandGroup heading="Suggestions">
              <CommandItem icon={<Calendar />} onSelect={() => setIsOpen(false)}>
                Calendar
              </CommandItem>
              <CommandItem icon={<Smile />} onSelect={() => setIsOpen(false)}>
                Search emoji
              </CommandItem>
            </CommandGroup>
          </CommandList>
        </CommandDialog>
      </>
    );
  }
  ```
</Example>

## Component props

### Command

<Example>
  <DataTable
    data={[
      { name: 'value', value: 'string', description: 'Controlled value of the selected item' },
      { name: 'onValueChange', value: '(value: string) => void', description: 'Called when the selected item changes' },
      { name: 'filter', value: '(value, search, keywords) => number', description: 'Custom filter function for ranking items' },
      { name: 'shouldFilter', value: 'boolean', description: 'Disable built-in filtering when false' },
      { name: 'loop', value: 'boolean', description: 'Loop keyboard navigation from last item to first' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>

### CommandInput

<Example>
  <DataTable
    data={[
      { name: 'placeholder', value: 'string', description: 'Input placeholder text' },
      { name: 'value', value: 'string', description: 'Controlled search value' },
      { name: 'onValueChange', value: '(search: string) => void', description: 'Called when the search value changes' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>

### CommandGroup

<Example>
  <DataTable
    data={[
      { name: 'heading', value: 'ReactNode', description: 'Group heading displayed above items' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>

### CommandItem

<Example>
  <DataTable
    data={[
      { name: 'icon', value: 'ReactNode', description: 'Icon displayed before the label' },
      { name: 'isDisabled', value: 'boolean', description: 'Disables the item' },
      { name: 'value', value: 'string', description: 'Value used for filtering (defaults to text content)' },
      { name: 'keywords', value: 'string[]', description: 'Additional keywords used when filtering' },
      { name: 'onSelect', value: '(value: string) => void', description: 'Called when the item is selected' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>

### CommandDialog

<Example>
  <DataTable
    data={[
      { name: 'isOpen', value: 'boolean', description: 'Whether the dialog is open' },
      { name: 'onOpenChange', value: '(open: boolean) => void', description: 'Called when the open state changes' },
    ]}
  >
    <DataColumn id="name" label="Name" />
    <DataColumn id="value" label="Type" />
    <DataColumn id="description" label="Description" />
  </DataTable>
</Example>
